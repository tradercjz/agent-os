# ── AgentOSConfigVersion.cmake ──
# Generated by CMakePackageConfigHelpers.  Do NOT edit manually.
#
# This file is meant to be consumed by find_package() in Config mode.
# It is generated at install time from CMakeLists.txt via
# write_basic_package_version_file().
#
# ────────────────────────────────────────────────────
# NOTE TO INTEGRATORS:
# ────────────────────────────────────────────────────
# This .in file is provided as a reference / fallback template.
# The RECOMMENDED way to generate the version file is to add the
# following snippet to the root CMakeLists.txt:
#
#   include(CMakePackageConfigHelpers)
#   write_basic_package_version_file(
#       "${CMAKE_CURRENT_BINARY_DIR}/AgentOSConfigVersion.cmake"
#       VERSION ${PROJECT_VERSION}
#       COMPATIBILITY SameMajorVersion
#   )
#   install(FILES
#       "${CMAKE_CURRENT_BINARY_DIR}/AgentOSConfigVersion.cmake"
#       DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/AgentOS
#   )
#
# The write_basic_package_version_file() call produces a proper
# version-check file automatically.  This .in template exists so
# that the cmake/ directory is self-documenting.

set(PACKAGE_VERSION "@PROJECT_VERSION@")

if(PACKAGE_VERSION VERSION_LESS PACKAGE_FIND_VERSION)
    set(PACKAGE_VERSION_COMPATIBLE FALSE)
else()
    set(PACKAGE_VERSION_COMPATIBLE TRUE)
    if(PACKAGE_FIND_VERSION STREQUAL PACKAGE_VERSION)
        set(PACKAGE_VERSION_EXACT TRUE)
    endif()
endif()
